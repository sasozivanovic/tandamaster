# Maintainer: Your Name <saso@milonguero.si>
pkgname=tandamaster
pkgver=0.2
pkgrel=1
epoch=
pkgdesc="A music player specialized for playing music at milongas"
arch=('any')
url=""
license=('GPL')
groups=()
depends=(
  'qt5-base>=5.4.1'
  'python-pyqt5'
  'ipython' # temporary, for debugging purposes
  'python-unidecode'
  'python-mutagen'
  'gstreamer'
  'gst-plugins-good'
  'libmp3splt'
  'python-systemd'
)
makedepends=()
checkdepends=()
optdepends=()
provides=()
conflicts=()
replaces=()
backup=()
options=()
install=
changelog=
#source=("$pkgname-$pkgver.tar.gz"
#        "$pkgname-$pkgver.patch")
#noextract=()
md5sums=()
validpgpkeys=()

#prepare() {
#	cd "$pkgname-$pkgver"
#	patch -p1 -i "$srcdir/$pkgname-$pkgver.patch"
#}

#build() {
#	cd "$pkgname-$pkgver"
#	./configure --prefix=/usr
#	make
#}

#check() {
#	cd "$pkgname-$pkgver"
#	make -k check
#}

#package() {
#	cd "$pkgname-$pkgver"
#	make DESTDIR="$pkgdir/" install
#}
